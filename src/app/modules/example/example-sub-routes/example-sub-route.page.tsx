import { Body } from '@atomic/atm.typography';
import { Grid } from '@atomic/obj.grid';
import type React from 'react';

const ExampleSubRoutePage: React.FC = () => {
  return (
    <Grid>
      <Body>Sub-route</Body>
    </Grid>
  );
};

export default ExampleSubRoutePage;
